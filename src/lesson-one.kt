fun variables() {
    /**
     * in kotlin, we define variables using either:
     * val -> immutable (read-only)
     * var -> mutable (can change)
     */

    val name = "John Doe"
    var age = 18
    age = 19

    println(name)
    println(age)
}

fun lesson_one() {
    variables()
}